import React from 'react';
import { SliderProps } from './slider_types';

const Slider: React.FC<SliderProps> = ({ ...rest }: SliderProps) => {
  return <div></div>;
};

Slider.defaultProps = {};
export default Slider;
